# Tasks

## 1. 输入资产

- [x] 1.1 增加文件哈希、JSON/IFC/DXF 检查器，并验证合法 JSON 与未知扩展名测试通过
- [x] 1.2 增加可选依赖声明和结构化示例输入，并验证 `pip install -e '.[dev]'` 成功

## 2. 运行生命周期

- [x] 2.1 增加能力探测、白名单运行目录和 JSON 运行清单，并验证越权路径被拒绝
- [x] 2.2 更新 MCP 网格、提交、状态、结果接口，并验证缺少后端时不伪造完成

## 3. 报告与回归

- [x] 3.1 增加 Markdown/HTML 报告生成和哈希返回，并验证失败报告保留限制声明
- [x] 3.2 运行全部 pytest、OpenSpec 校验和 Git 状态检查，确认结果可复现
